# Dataset — TweetEval Sentiment

Source: Hugging Face Datasets dataset `tweet_eval`, configuration `sentiment`.

Runtime loading:
```python
load_dataset("tweet_eval", "sentiment")
```

No dataset rows are committed. Preserve the upstream dataset card/license/citation requirements. The supplied train/validation/test splits are retained; do not concatenate and randomly resplit them because that breaks benchmark comparability and can increase near-duplicate leakage risk.
