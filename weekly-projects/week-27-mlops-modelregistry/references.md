# References

- MLflow Model Registry documentation (current 2026): centralized model lifecycle with lineage, versioning, aliases, tags and annotations.
- MLflow 3 tracking/model documentation: models are first-class tracking entities and may carry checkpoint/metric lineage.
- MLflow Model Registry Workflows: aliases such as `champion` decouple deployment references from fixed version numbers.
